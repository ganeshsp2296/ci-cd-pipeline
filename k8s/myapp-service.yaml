apiVersion: v1
kind: Service
metadata:
  name: myapp-service
spec:
  selector:
    app: myapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080   # your app container port
      nodePort: 31000    # custom NodePort (not conflicting with Jenkins/Nexus/SonarQube)
  type: NodePort

